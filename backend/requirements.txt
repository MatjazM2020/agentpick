# Web framework (OpenAI-compatible API)
FastAPI
uvicorn
pydantic
python-dotenv

# Agent (Microsoft Agent Framework over OpenAI Chat Completions)
agent-framework

# Catalog data sources
qdrant-client        # semantic search over model-card chunks
psycopg2-binary      # model metadata (PostgreSQL)

# Query embeddings for semantic search
sentence-transformers
